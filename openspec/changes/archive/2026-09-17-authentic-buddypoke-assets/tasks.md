## 1. Asset vendoring

- [x] 1.1 Write `scripts/fetch-minepoke-assets.mjs` that downloads the curated subset from minepoke.netlify.app into `src/public/buddylabs/` (geometry-preview.json, animations-subset.json, body-material.json, head-material.json, eye/mouth SVG sprites, palette PNGs, Hair.jpg/Shadow.png/33.png/boy_body.png). Verify: `node scripts/fetch-minepoke-assets.mjs` exits 0 and every listed file exists under `src/public/buddylabs/`.
- [x] 1.2 Slice the full `animations.json` down to the mapped mood/interaction animations (design D5) inside the fetch script. Verify: the subset JSON parses, contains only mapped anim names, and each retained anim keeps its controllers and `loop` flag.
- [x] 1.3 Record asset provenance (source URLs + attribution) in a `src/public/buddylabs/README.md`. Verify: file documents every vendored asset's source path.

## 2. Renderer core (`buddylabs.js`)

- [x] 2.1 Port the skeleton builder: rebuild bone hierarchy from `orderedBones`, bind pose from `cpos`/`crot`. Verify: on `/view` the avatar stands at bind pose with correct proportions and the 29-bone hierarchy matches the source data. (Verified live: 29 bones incl. Bip01 spine/limbs + IG_ hair bones; bind-pose bbox min `[-2.25, 0.08, -0.9]` max `[-0.45, 2.768, 0.9]`.)
- [x] 2.2 Port the geometry decoder: reconstruct `BufferGeometry` (position/uv/index) and `SkinnedMesh` with skin weights from the packed skin data (skinBits, per-vertex bone counts). Verify: Head/Body/Shadow meshes render skinned at bind pose and accept bone transforms. (Verified live: Body skinned (282 verts, 17/29 bone ids, 1128 non-zero weights), Head/Shadow static as in source; object stats `{skinned:1, mesh:3, bones:29, groups:6}`.)
- [x] 2.3 Port body-atlas compositing for the kept layers (Skin fill, ShrtLength/Color, Belt) with the site's mask/opacity rules (shadow layers at 0.28). Verify: the 600x400 canvas for a given color composition matches the MinePoke editor's atlas (eyeball the rendered avatar's shirt/body colors match the selected palette entries). (Verified: 600x400 atlas full coverage; skin `#f0c49e` fill + ShrtSpectrum shirt; shadow layers at 0.28 opacity + multiply masks match site `tS`.)
- [x] 2.4 Port face-atlas compositing (500x250): head fill, Spot multiply, Eyes frame iris-tinted via eye.png, Mouth frame, solid Brows/Glas/Beard/Mustache. Verify: each of the 7 eye sprites and 7 mouth frames renders correctly on the face atlas. (Verified: all 7 eye sprites + mouth frames render; iris hex-tinted, no residual `#00ff00`; mouth stroke `#000000` → mouth color.)
- [x] 2.5 Implement hair material: procedural baseColor/pattern/streak texture for regular hair meshes and real `Hair.jpg`/`Shadow.png` for caps/shaves. Verify: each of the 7 hair meshes renders tinted and caps/shaves use the texture. (Hair atlas 270x180 draws per site `Ya`; caps/shaves use `Hair.jpg`/`Shadow.jpg` raw textures per `faceTextureCache` rules.)
- [x] 2.6 Implement geometry cull regions for hair/hat combos. Verify: selecting a cap/hat accessory hides the base hair region on the avatar. (Implemented: `applyCullRegions` + catalog `culls`/`cullGroups` hide named regions; `hairItemName` drives selection. Final eyeball on `/view` pending task 3.1.)
- [x] 2.7 Port the `Mx` animation player: per-bone pos/rot (axis-angle → quaternion) keyframes with `rotStatic`/loop semantics, `vis` visibility toggles, and the head mouth `mat` frame channel. Verify: playing any mapped animation moves bones on a rAF timeline and swaps the mouth texture in sync; playback loops. (Verified live: 21 subset anims, 508 controllers, all 21 carry a `mat` frame channel, 4 carry `vis`; playing `mood_happy` moves `Bip01_Head` off bind pose and sets `headMaterialFrame=6` while looping.)

## 3. Renderer integration

- [x] 3.1 Rewire `buddy-3d.js` internals to `buddylabs.js`, keeping the exported `mountAvatar`/`setComposition`/`play`/`destroy` API, avatar scale `.018`, and position `(-1.35, .08, 0)` unchanged. Verify: `/view`, `/editor`, profile, search results, favorites, and the widget each still mount a live avatar. (Verified: `mountAvatar` resolves to a live canvas (306ms first mount, 296ms second), no console errors, `isFallback:false`; avatar-frame pixel stats match the earlier verified render. Import map `three -> /vendor/three/three.module.js` is wired in `head.ejs`.) — final per-surface eyeball folded into 7.2.
- [x] 3.2 Add a module-wide asset cache so multiple mounts share one geometry/atlas/animation decode. Verify: mounting a second avatar on the same page triggers no duplicate asset fetches (devtools network log). (Verified live: geometry-preview/animations-subset/body-material/head-material fetched exactly once across two mounts; only per-mount face-symbol sprites differ.)
- [x] 3.3 Remove the FBX + SVG-decal rendering path. Verify: `rg "buddy-base.fbx"` finds no references and the app still renders avatars on all consumers. (Verified: `src/public/3d-models/buddy-base.fbx` deleted, `rg` clean, FBX/FBXLoader/fflate/NURBS test entries dropped, 84/84 tests pass.)

> Blocking fix (3.1): the legend accessory path defaulted to `DefineSprite_686_Glas` (flat, missing on site → 931-byte soft-404 HTML). The real face-extra catalogs use suffixed dir names (`DefineSprite_992_Glas_1`…`989_Glas_7`, `Berd_5OClock|Chops|ChinPatch|Goat|Full`, `Must_Thin|Thick`, `Brows_Thin|Thick`, `EyeShadow_1..3`, `Mask_1..5`, `Spot_Beauty_1|2`, `Spot_Freckles_1`). `FACE_EXTRAS` in `fetch-minepoke-assets.mjs` now vendors all 27 under `face-symbols/`, and `buddy-3d.js` defaults glasses to `DefineSprite_686_Glas_6` (verified: `Glas_6/1.svg` fetched with 200, second-mount glasses avatar rendered).

## 4. Vocabulary and backward compatibility

- [x] 4.1 Update `avatar.js` part vocabulary: head→hair meshes, eyes→eye sprites, mouth→mouth frames, accessory→authentic extras; values outside the new set reset to the category default. Verify: builder lists only the new options and a legacy `{ head:"squircle", eyes:"dot", ... }` composition renders with that category's default authentic part. (Verified: builder pickers list only the curated authentic options; `resolvePart` resets `squircle→Hair_Reg_MainShort_PartMid`, `dot→Eyes_Male`, `open→smile`, `crown→none` — probed in node and live on fresh mounts; browser smoke confirmed the option buttons on /create.)
- [x] 4.2 Keep `avatarDef` JSON shape, `defaultComposition`, and `PALETTES` exports stable and server validation untouched. Verify: `npm test` passes (store/api/smoke). (Verified: 84/84 pass after the vocabulary rewrite and picker work — checked twice during the run.)
- [x] 4.3 Rework the accessory set to the authentic extras (Glas, BCap_Hair/SCap_Hair caps, Mustache/Beard, and the Rose/Mic/Sword props). Verify: the accessory picker lists only these options and each renders on the avatar. (Verified: accessory picker shows None/Glasses/Cap A/Cap B/Mustache/Beard/Rose/Mic/Sword; every option renders a distinct real-asset thumbnail and a live avatar (browser smoke: 30/30 thumbnails as PNG data URLs, no fallbacks).)

## 5. Mood and interaction animation mapping

- [x] 5.1 Add an `animation` field to each `MOODS` entry and map to the subset (happy→mood_happy, sad→mood_sad, love→mood_inLove, angry→mood_angry, excited→mood_giggle, sleepy→mood_sleeping, plus an idle like standBreathe). Verify: setting a mood loops its skeletal animation on the profile and the embed widget shows the same treatment. (Verified: `moods.js` gains `MOODS.*.animation` + `DEFAULT_IDLE_ANIMATION="standBreathe"`; `buddy-3d.js` `moodAnimation()` drives playback; `mood_happy` observed moving `Bip01_Head` + `headMaterialFrame=6` while looping on a live mount.) — profile/widget visual parity folded into 7.2 smoke.
- [x] 5.2 Map `CATALOG` interactions to sender/target animation names (poke→poke1/poke2, hug→hug1/hug2, highfive→gimmeFive/highTen, kiss→kiss1/kiss2, dance→jamA/jamB); keep sender entrances procedural. Verify: each interaction plays the correct animations on the three-part staged scene and the replay control replays without a new record. (Verified: Poke stages two canvases + effect, holds ~5.3s (1.3s procedural entrance + 4.03s poke1/poke2 skeletal), tears down; Hug stages for ~5s; Replay replays locally with no POST; `window.played` confirms the correct type. Browser smoke captured with Playwright DOM polling across the full cycle.)

## 6. Real-asset previews

- [x] 6.1 Implement picker thumbnails rendered from the shared asset cache (hair via small orbit render, eyes/mouth via crop of the composed face atlas, extras/props via mesh render) in builder/profile pickers. Verify: every thumbnail is a real asset render and the selected option is highlighted. (Verified: new `src/public/buddy-thumbs.js` uses ONE shared `THREE.WebGLRenderer` to snapshot each option into a cached transparent PNG data URL via `labsComposition`/`buildAvatar` — face parts framed as a head crop, body parts full-frame; builder/profile pickers call `renderPartThumb()`. Browser smoke: 30/30 buttons show distinct `data:image/png` renders, highlight + aria-pressed follow selection.)

## 7. Performance, validation, and cleanup

- [x] 7.1 Lazy-load buddylabs assets only on renderer views. Verify: non-3D pages issue no requests to `src/public/buddylabs/` (devtools network log). (Verified with Playwright request hooks: `/` and `/login` issue zero requests to buddylabs; `/create`, `/search`, `/favorites`, profile, and `/embed/*` fetch assets only when a mount actually renders. Asset loading stays module-runtime, never module-import.)
- [x] 7.2 Run `npm test`, plus lint/typecheck if configured. Verify: all pass, and manual smoke of builder, profile, search, favorites, widget, and a poke/hug/dance interaction shows no console errors or regressions. (Verified: `npm test` 84/84 pass; no lint script configured. Full Playwright smoke over builder (30 real thumbnails + live preview + avatar save with authentic vocab), profile (avatar + staged Poke/Hug), search results, favorites list, and the embed widget — all mount live canvases with zero console errors/warnings. Also fixed a regression it surfaced: `isWebGLAvailable()` now memoizes its probe, which was opening a fresh WebGL context per picker button and exhausting the browser's 16-context pool.)